# kb/ 值班手册交叉一致性审查报告

- 审查范围：`kb/` 下 01–18 共 18 篇值班手册（只读审查，未修改任何手册）。
- 审查项：① 交叉引用有效性；② 跨篇事实一致性（阈值/配额/保留期/组名/路径）；③ 同主题表述互补性（OOM、权限、镜像源、占卡）；④ 命令行示例语法。
- 审查日期：以审查执行日为准。

---

## 一、问题清单（按严重度排序）

### 【高】1 项

**H1. 「高显存 + 低利用率」占卡监控阈值存在两套互相矛盾的标准**

- `kb/09-GPU使用规范与排队约定.md`（卡配额与默认限制一节）：
  > 显存长期占用超过 **90%** 且利用率低于 **20%** 的任务会被监控标记，值班员会提醒优化
- `kb/13-监控告警与值班响应规范.md`（常见告警项与阈值一节）：
  > GPU 显存占用 **> 95%** 持续 30 分钟且利用率 **< 10%**：疑似残留/占卡，触发提醒

两篇描述的是同一类监控判定（高显存占用且低利用率 → 值班员介入提醒），但阈值完全不同（90%/20% vs 95%/10%）。按 09 的标准，一个显存占 92%、利用率 15% 的任务应被标记；按 13 的标准则不触发。值班员执行时无所适从，属于跨篇事实冲突，需统一为一套阈值（或明确写成两个不同等级的告警项并各自命名）。

### 【中】1 项

**M1. 02 手册引用的「GPU/内存排查手册」不存在，属悬空引用**

- `kb/02-训练任务异常排查.md`（任务报错退出一节）：
  > 若日志无 error 只有 info，说明不是应用层报错，应转向查进程资源占用与系统层日志（对应 GPU/内存排查手册）。

kb/ 下不存在任何「内存排查手册」。最接近的 `01-GPU显存与占卡排查.md` 只覆盖显存（GPU 侧），不覆盖内存；而内存侧排查（OOM Killer、`dmesg | grep -i kill`）其实就写在 02 自己开篇。该引用无真实目标，读者按图索骥会落空。建议改为明确指向 01（显存方向）并说明内存方向见本篇 OOM Killer 一节。

### 【低】5 项

**L1. `git lfs pull --include` 缺必需的模式参数，原样照抄会报错**

- `kb/16-模型下载与缓存管理.md`（git lfs 与大文件问题一节）：
  > 必须用 git 的场景：`GIT_LFS_SKIP_SMUDGE=1 git clone` 先拉结构，再对需要的大文件单独 `git lfs pull --include`。

`--include` 是必须带值的选项（如 `git lfs pull --include="*.bin"`），按原文逐字执行会因缺少参数报错。建议补一个示例模式。

**L2. 09 手册「上限 4 张」与「超过 4 张需报备」存在篇内张力**

- `kb/09-GPU使用规范与排队约定.md`：
  > 单人同时占用超过 4 张卡需提前报备说明用途。（占卡冲突协调一节）
  > 每人默认上限 4 张卡；组内共享的 A100 节点默认每人 2 张，超额使用需导师审批。（卡配额与默认限制一节）

既然「上限 4 张」，就不存在「超过 4 张需报备」的情形；结合后文「超额使用需导师审批」可推断本意是「默认 4 张，超额走报备+审批」，但「上限」一词与之矛盾。属篇内表述问题，不影响跨篇一致性，建议改「上限」为「默认额度」。

**L3. Xid 79（掉卡）的报修时机两篇不一致**

- `kb/06-CUDA驱动与显卡状态排查.md`（Xid 错误码速查一节）：
  > **Xid 79**：GPU has fallen off the bus，掉卡，多与供电或过热有关，**需要断电重启并报修检测**。
- `kb/17-温度功耗与硬件报修.md`（硬件故障迹象识别一节）：
  > `nvidia-smi` 报 `GPU has fallen off the bus`（Xid 79）：掉卡，**重启后若复现即报修**；

06 的口径是重启与报修并行（重启是处置动作，报修不以其结果为前提），17 的口径是「复现才报修」。值班员按不同手册会得到不同的报修门槛，建议统一（例如：重启后复现即报修；若反复掉卡直接停用报修）。

**L4. 17 手册「主动停掉部分低优先级训练」未提确认流程，与高危操作原则有轻微张力**

- `kb/17-温度功耗与硬件报修.md`（机房环境异常一节）：
  > 多节点同时高温告警基本是机房空调问题（P0 级）：立即在群里通知所有人暂停新任务，联系机房管理员，**必要时主动停掉部分低优先级训练降载**。
- 对照 `kb/18-值班处理与升级流程规范.md`（高危操作确认机制）：
  > 禁止值班员在无确认的情况下 kill 任何非本人进程，哪怕"看起来明显是残留"。
- 对照 `kb/09-GPU使用规范与排队约定.md`（违规处理升级）：
  > 所有清理动作（kill 进程）都属于高危操作，值班员只发起申请，由本人或管理员执行。

「停掉他人训练」属杀他人进程，17 未说明由谁执行、是否需要确认。P0 紧急场景或许需要例外通道，但 18 的高危操作原则没有为 P0 留例外说明，两篇衔接处有空白。建议 17 补一句执行主体与确认方式（如「由管理员执行」或「P0 可先行停载、事后补登记」）。

**L5. home 超限后的迁移目标路径粒度不一**

- `kb/12-账号权限与配额管理.md`（home 配额超限的表现与处理一节）：
  > 缓存类直接清（`pip cache purge`、`conda clean -a`），数据类迁移到 **/data** 并软链接回来。
- 对照 `kb/11-数据集与共享目录管理.md`：
  > 数据、缓存、conda 环境都应放 `/data/users/<用户名>`，再软链接回 home
- 对照 `kb/16-模型下载与缓存管理.md`：
  > 设置 `export HF_HOME=/data/users/<用户名>/hf_cache`，让缓存落到个人数据区

11 与 16 都明确指向 `/data/users/<用户名>`（个人数据区），12 只写「迁移到 /data」，粒度偏粗，可能误导用户把数据直接堆到 /data 根目录（与 11 的目录结构约定不符）。属措辞精度问题，不算事实冲突。

---

## 二、交叉引用核对明细（检查点①）

全库共 8 处显式交叉引用，7 处有效，1 处悬空（即 M1）：

| 出处 | 引用原文 | 指向 | 核对结果 |
| --- | --- | --- | --- |
| 02-训练任务异常排查 | 「对应 GPU/内存排查手册」 | 无此手册 | ✗ 悬空（见 M1） |
| 10-网络与SSH连接排查 | 「训练任务必须放 tmux/screen…（参见 GPU 使用规范）」 | 09「必须在 tmux 或 screen 会话中运行，防止 SSH 断连」 | ✓ 存在且内容对得上 |
| 10-网络与SSH连接排查 | 「HuggingFace 模型下载走镜像站（详见模型下载与缓存管理手册）」 | 16「export HF_ENDPOINT=https://hf-mirror.com」 | ✓ 存在且内容对得上 |
| 11-数据集与共享目录管理 | 「参见 Docker 手册的属主问题一节」 | 08「容器内 root 写共享卷的属主问题」一节 | ✓ 存在且章节对得上 |
| 11-数据集与共享目录管理 | 「home 目录配额只有 50GB（详见账号配额手册）」 | 12「默认配额：home 50GB、/data/users/<用户名> 2TB」 | ✓ 存在且数字一致 |
| 15-Jupyter与远程开发排查 | 「放 tmux 里跑并输出日志（参见 GPU 使用规范）」 | 09「日志重定向到文件（> train.log 2>&1）」 | ✓ 存在且内容对得上 |
| 16-模型下载与缓存管理 | 「排查 home 超限时…（参见配额手册）」 | 12「home 配额超限的表现与处理」一节 | ✓ 存在且内容对得上 |
| 17-温度功耗与硬件报修 | 「ECC Aggregate 错误计数持续增长（参见 CUDA 驱动手册）」 | 06「ECC 错误计数检查」一节 | ✓ 存在且内容对得上 |

另有一处隐式引用：15「按占卡冲突流程联系本人」指向 09「占卡冲突协调」一节（30 分钟无响应可申请管理员清理），内容吻合 ✓。

## 三、跨篇事实核对明细（检查点②）

以下事实项跨篇出现且**核对一致**：

- **配额**：home 50GB（11、12、16 三处一致）；`/data/users/<用户名>` 2TB（11、12 一致）。
- **组名**：公共数据集统一 `data` 组只读（11「统一 data 组只读」「groups 确认自己是否在 data 组」；12「默认加入 data 组」）✓。
- **目录路径**：`/data/datasets`（公共数据集，只读：11、14）；`/data/checkpoints`（不备份、定期清理：11、14）；`/data/models`（公共模型区，只读共享：11、16）；`/data/users/<用户名>` 不备份（14）与各篇使用方式不冲突；docker 数据目录 `/data/docker`（08，仅此一处提及）。
- **备份/保留期**（14 篇内自洽，无跨篇矛盾）：`/data/datasets` 每周快照保留 4 份 ↔「只能找回 4 周内」；home 每日增量保留 7 天 ↔「只能找回 7 天内」。
- **温度阈值**：85℃ 降频、90℃ 告警（17），与 13「GPU 温度 > 90℃ 高温告警」一致 ✓。
- **P0 定义**：13（节点失联、sshd 无法登录、机房断电/空调、共享存储不可写）、18（节点失联、共享存储不可写、机房环境异常）、10（sshd 无法 fork 属 P0）、17（机房空调问题 P0）四处口径一致 ✓。
- **Xid 知识**：Xid 31（用户代码 bug）、Xid 48（ECC 硬件问题，停调度报修）、ECC Volatile 双比特 >0 告警（06）与 13「单卡异常（Xid 报错、掉卡）列 P1」、17「ECC Aggregate 持续增长属硬件迹象」互相吻合 ✓。
- **CUDA/驱动兼容口径**：03（按驱动支持的最高 CUDA 版本装 PyTorch）、06（驱动决定最高 CUDA 版本，535 驱动支持 CUDA 12.2 及以下；用户侧降版本而非升驱动）、08（镜像内 CUDA runtime 不得高于宿主驱动上限）三篇一致 ✓（06 的「535 ↔ CUDA 12.2」对应关系本身也正确）。
- **高危操作边界**：01（清理僵尸进程需人工确认）、05（清理他人文件需确认）、06（重启由管理员安排）、08（`docker system prune` 须确认后由管理员执行）、09（kill 由本人或管理员执行）、13（高危只出建议）、18（高危三类 + 确认机制）七篇口径一致 ✓。
- **离组/账号**：12「冻结 3 个月再删除」、14「毕业季 6-7 月删除谨慎」互补，无冲突 ✓。

**发现的冲突**见问题清单 H1（唯一一项硬性数值冲突）；L2–L5 为篇内张力或表述粒度问题。

## 四、同主题互补性核对（检查点③）

- **OOM**：显存 OOM（`CUDA out of memory`，batch 过大/残留进程 → 01、04）与系统内存 OOM（OOM Killer，`num_workers`/数据一次性读入 → 02、04、15 kernel 超 cgroup 配额）分野清晰；07 补充分布式场景下「先挂的 rank 常被 OOM 杀掉、退出码 137 = SIGKILL」；04 对照表与 01/02 原文逐条吻合。各篇**互补无矛盾** ✓。
- **权限**：03（不要 sudo 装包，用 `pip install --user`/个人 conda 环境）与 12（普通用户一律无 sudo，系统库找管理员或 conda-forge）一致；05（共享数据集只读挂载）、11（data 组成员资格、`chmod g+r` 共享）、08（容器 root 属主问题及 `-u $(id -u):$(id -g)` 规范）各管一段，**互补无矛盾** ✓。
- **镜像源/下载加速**：10（pip 换清华 tuna 源、conda tuna channels、apt 源不要自行改、HF 走镜像站并指向 16）与 16（`HF_ENDPOINT=https://hf-mirror.com`、gated 模型 token、401/403 区分）衔接自然，**互补无矛盾** ✓。
- **占卡**：01（技术排查：定位 PID/占用者）、09（流程规范：报备、空闲判定 <500MB、冲突协调、违规升级）、13（监控告警侧）、15（Jupyter kernel 残留是头号来源，按 09 流程处理）构成「技术—流程—监控—典型场景」的完整闭环，**除 H1 的阈值冲突外无矛盾**。
- **性能优化阈值**（非冲突说明）：01「利用率长期低于 30% 但显存占满 → DataLoader 瓶颈」是优化建议语境，与 09/13 的监控告警语境不同，不算冲突；但 09 与 13 同为监控判定，故 H1 成立。

## 五、命令行示例语法核查（检查点④）

逐条核对了 18 篇中全部命令示例，除 L1 外语法均正确。重点验证过的易错项：

- 02/07：`py-spy dump --pid <PID>` ✓；07「退出码 137 = 被 SIGKILL」（128+9）✓
- 06：`ps aux | awk '$8 ~ /D/'`（$8 确为 STAT 列）✓；`rmmod nvidia_uvm nvidia_drm nvidia_modeset nvidia`（卸载顺序正确）✓；`nvidia-smi -q -d ECC` ✓
- 07：`ss -tlnp | grep <端口>`、`NCCL_DEBUG=INFO`、`NCCL_SOCKET_IFNAME`、`NCCL_IB_DISABLE=1` ✓
- 08：`--gpus '"device=2,3"'`（引号写法正确）✓；`--shm-size=16g`；容器默认 shm 64MB 的描述属实 ✓；`-u $(id -u):$(id -g)` ✓
- 10：`pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple` ✓；`-o GSSAPIAuthentication=no`、`ServerAliveInterval 60` ✓
- 11：`rsync -avP`、`ln -s /data/users/foo/envs ~/envs` ✓
- 12：`du -sh ~/.[!.]* ~/* | sort -h`（点文件 glob 写法正确）✓；`pip cache purge`、`conda clean -a` ✓
- 15：`ssh -L 8888:localhost:8888 user@server` ✓；`pkill -u $(whoami) -f vscode-server` ✓
- 16：`export HF_ENDPOINT=https://hf-mirror.com`、`GIT_LFS_SKIP_SMUDGE=1 git clone` ✓；`huggingface-cli download/login` 仍可用（新版已改名 `hf`，不影响正确性）；唯一问题为 `git lfs pull --include` 缺参数（L1）
- 17：`nvidia-smi -q -d TEMPERATURE,PERFORMANCE`（两个 -d 单元均合法）✓；`ipmitool sdr | grep -i fan`、`lspci | grep -i nvidia` ✓

## 六、总体一致性评价

**整体质量较高，可以定级为「基本一致，少量必修」。**

- 18 篇手册形成了一套自洽的体系：统一的高危操作确认原则在 7 篇中口径一致；配额（home 50GB / 个人区 2TB）、组名（data 组）、目录约定（datasets/checkpoints/models/users 四区）跨篇完全吻合；P0/P1/P2 分级在 4 篇中互相呼应；04 错误对照表与各专题手册逐条吻合。
- 交叉引用 8 处中 7 处真实有效且内容对得上，仅 02 一处悬空引用（M1）。
- 同主题（OOM、权限、镜像源、占卡）的多篇表述以互补为主，唯一的硬性矛盾是 09 与 13 的占卡监控阈值双标准（H1），这也是全库唯一可能导致值班员实际操作分歧的问题，**建议优先修复**。
- 命令示例质量高，18 篇中仅 16 的 `git lfs pull --include` 缺参数一处瑕疵（L1）。
- 修复优先级建议：H1（统一阈值）→ M1（修引用）→ L3（统一 Xid 79 报修口径）→ L4（P0 停载补确认说明）→ L2/L5/L1（措辞与示例修补）。
